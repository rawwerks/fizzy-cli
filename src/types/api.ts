/**
 * TypeScript types for Fizzy API responses
 *
 * These types are automatically inferred from Zod schemas in ../schemas/api.ts
 * Import types from this file for type-safe API interactions.
 */

export type {
  User,
  Account,
  IdentityResponse,
  Board,
  Step,
  Card,
  CommentBody,
  Comment,
  Reaction,
  Tag,
  Column,
  NotificationCard,
  Notification,
} from '../schemas/api';
